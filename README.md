#  Customer Shopping Behaviour Analysis — SQL Project

An **Customer Behaviour Analysis project** built using **PostgreSQL** to analyze customer purchasing behavior, revenue, discounts, product performance, shipping preferences, subscriptions, and customer segments.

The project answers **10 real-world business questions** using SQL techniques such as aggregation, filtering, subqueries, CTEs, `CASE` statements, window functions, and grouping.

##  Project Overview

The objective of this project is to transform raw customer transaction data into meaningful business insights.

The analysis focuses on:

*  Revenue and customer spending
*  Customer demographics
*  Discount usage
*  Product review ratings
*  Shipping preferences
*  Subscription behavior
*  Customer retention and segmentation
*  Product purchasing patterns
*  Age-group revenue contribution

##  Dataset

**Table:** `customer`

The dataset contains customer-level information related to:

* Customer ID
* Gender
* Age / Age Group
* Product purchased
* Category
* Purchase amount
* Review rating
* Discount applied
* Shipping type
* Subscription status
* Previous purchases

## Tools & Technologies

| Tool / Technology     | Purpose                                   |
| --------------------- | ----------------------------------------- |
| **PostgreSQL**        | Database and SQL analysis                 |
| **SQL**               | Data querying and analysis                |
| **pgAdmin / DBeaver** | SQL development environment               |
| **GitHub**            | Project documentation and version control |

### SQL Concepts Used

* `SELECT`
* `WHERE`
* `GROUP BY`
* `ORDER BY`
* Aggregate functions: `SUM()`, `AVG()`, `COUNT()`
* `ROUND()`
* `CASE`
* Subqueries
* Common Table Expressions (CTEs)
* Window Functions
* `ROW_NUMBER()`
* Filtering and conditional aggregation

---

# ❓ Business Questions & SQL Analysis

## Q1. What is the total revenue generated by male vs. female customers?

This query calculates total purchase revenue grouped by gender.

```sql
SELECT 
    gender, 
    SUM(purchase_amount) AS revenue
FROM customer
GROUP BY gender;
```

**Business Insight:**
Helps compare purchasing contribution between male and female customers.

---

## Q2. Which customers used a discount but still spent more than the average purchase amount?

```sql
SELECT 
    customer_id, 
    purchase_amount 
FROM customer 
WHERE discount_applied = 'Yes' 
  AND purchase_amount >= (
      SELECT AVG(purchase_amount) 
      FROM customer
  );
```

**Business Insight:**
Identifies customers who took advantage of discounts while still making relatively high-value purchases.

---

## Q3. Which are the top 5 products with the highest average review rating?

```sql
SELECT 
    item_purchased,
    ROUND(AVG(review_rating::numeric), 2) AS "Average Product Rating"
FROM customer
GROUP BY item_purchased
ORDER BY AVG(review_rating) DESC
LIMIT 5;
```

**Business Insight:**
Highlights highly rated products that may deserve additional marketing or inventory attention.

---

## Q4. Compare the average Purchase Amounts between Standard and Express Shipping.

```sql
SELECT 
    shipping_type, 
    ROUND(AVG(purchase_amount), 2) AS average_purchase_amount
FROM customer
WHERE shipping_type IN ('Standard', 'Express')
GROUP BY shipping_type;
```

**Business Insight:**
Determines whether customers choosing different shipping options have different average spending levels.

---

## Q5. Do subscribed customers spend more?

The analysis compares customer count, average spending, and total revenue between subscribers and non-subscribers.

```sql
SELECT 
    subscription_status,
    COUNT(customer_id) AS total_customers,
    ROUND(AVG(purchase_amount), 2) AS avg_spend,
    ROUND(SUM(purchase_amount), 2) AS total_revenue
FROM customer
GROUP BY subscription_status
ORDER BY total_revenue, avg_spend DESC;
```

**Business Insight:**
Helps evaluate whether subscription programs are associated with higher customer spending and revenue.

---

## Q6. Which 5 products have the highest percentage of purchases with discounts applied?

```sql
SELECT 
    item_purchased,
    ROUND(
        100.0 * SUM(
            CASE 
                WHEN discount_applied = 'Yes' THEN 1 
                ELSE 0 
            END
        ) / COUNT(*), 
        2
    ) AS discount_rate
FROM customer
GROUP BY item_purchased
ORDER BY discount_rate DESC
LIMIT 5;
```

**Business Insight:**
Identifies products that are most frequently purchased using discounts, which can help evaluate promotional strategies.

---

## Q7. Segment customers into New, Returning, and Loyal.

Customers are categorized based on their number of previous purchases.

```sql
WITH customer_type AS (
    SELECT 
        customer_id,
        previous_purchases,
        CASE 
            WHEN previous_purchases = 1 THEN 'New'
            WHEN previous_purchases BETWEEN 2 AND 10 THEN 'Returning'
            ELSE 'Loyal'
        END AS customer_segment
    FROM customer
)

SELECT 
    customer_segment,
    COUNT(*) AS "Number of Customers"
FROM customer_type 
GROUP BY customer_segment;
```

### Customer Segments

| Previous Purchases | Segment   |
| -----------------: | --------- |
|                  1 | New       |
|               2–10 | Returning |
|                11+ | Loyal     |

**Business Insight:**
Provides a simple customer lifecycle segmentation that can support retention and marketing strategies.

---

## Q8. What are the top 3 most purchased products within each category?

This analysis uses a **CTE and `ROW_NUMBER()` window function** to rank products within each category.

```sql
WITH item_counts AS (
    SELECT 
        category,
        item_purchased,
        COUNT(customer_id) AS total_orders,
        ROW_NUMBER() OVER (
            PARTITION BY category 
            ORDER BY COUNT(customer_id) DESC
        ) AS item_rank
    FROM customer
    GROUP BY category, item_purchased
)

SELECT 
    item_rank,
    category,
    item_purchased,
    total_orders
FROM item_counts
WHERE item_rank <= 3;
```

**Business Insight:**
Shows the best-selling products within every category and helps identify category-specific customer preferences.

---

## Q9. Are repeat buyers also likely to subscribe?

Repeat buyers are defined as customers with more than 5 previous purchases.

```sql
SELECT 
    subscription_status,
    COUNT(customer_id) AS repeat_buyers
FROM customer
WHERE previous_purchases > 5
GROUP BY subscription_status;
```

**Business Insight:**
Helps determine the relationship between customer loyalty and subscription adoption.

---

## Q10. What is the revenue contribution of each age group?

```sql
SELECT 
    age_group,
    SUM(purchase_amount) AS total_revenue
FROM customer
GROUP BY age_group
ORDER BY total_revenue DESC;
```

**Business Insight:**
Identifies which age groups contribute the most revenue and can help businesses target their marketing efforts.

---

#  Key Analysis Areas

The project covers several important e-commerce analytics dimensions:

###  Revenue Analysis

* Revenue by gender
* Revenue by age group
* Revenue by subscription status

###  Product Analysis

* Highest-rated products
* Most purchased products
* Discount-heavy products

###  Customer Analysis

* New vs. returning vs. loyal customers
* Repeat buyer behavior
* Subscription adoption

###  Purchase Behavior

* Standard vs. Express shipping
* Discount usage
* Average purchase amount

---

#  Key SQL Skills Demonstrated

This project demonstrates practical knowledge of:

**1. Aggregation**

```sql
SUM()
AVG()
COUNT()
```

**2. Conditional Logic**

```sql
CASE WHEN ... THEN ... ELSE ... END
```

**3. Subqueries**

```sql
WHERE purchase_amount >= (
    SELECT AVG(purchase_amount)
    FROM customer
)
```

**4. CTEs**

```sql
WITH customer_type AS (...)
```

**5. Window Functions**

```sql
ROW_NUMBER() OVER (
    PARTITION BY category
    ORDER BY COUNT(customer_id) DESC
)
```

**6. Data Segmentation**

Customers are classified into meaningful groups based on purchasing behavior.

---

#  How to Run the Project

### 1. Install PostgreSQL

Create a PostgreSQL database and connect using **pgAdmin**, **DBeaver**, or another PostgreSQL-compatible SQL client.

### 2. Import the Dataset

Load the customer dataset into your PostgreSQL database.

### 3. Create the Table

Make sure the table is named:

```text
customer
```

### 4. Run the SQL Queries

Open the SQL script and execute each query individually to reproduce the analysis.

### 5. Analyze the Results

Review the output of each query and use the results to understand customer purchasing behavior and business performance.

---

# Project Structure

```text
customer_behaviour-analysis/
│
├── data/
│   └── customer.csv
│
├── sql/
│   └── customer_behaviour_analysis.sql
│
└── README.md
```

---



#  Project Summary

This project demonstrates how **SQL can be used to answer practical business questions from customer_behaviour data**.

From revenue analysis and product performance to customer segmentation and subscription behavior, the project provides a strong foundation for **data analytics and business intelligence** using PostgreSQL.

**Skills:** `SQL` · `PostgreSQL` · `Data Analysis` · `Customer Analytics` · `Business Intelligence` · `CTEs` · `Window Functions`
